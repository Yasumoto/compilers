! This is a comment. It continues to the end-of-line.
let
	type apple ~ class
		var color : Integer;

		proc set_color(set_to : Integer) ~
			color := set_to;

		proc print_color() ~
			putint(color);
		end;

	var y : apple
in
       begin
       y.set_color(1)
       y.print_color()
       end
